_schema-version: "3.2"
ID: login
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: login-destination-content
  type: com.sap.application.content
  requires:
  - name: login-destination-service
    parameters:
      content-target: true
  - name: login-repo-host
    parameters:
      service-key:
        name: login-repo-host-key
  - name: login-uaa
    parameters:
      service-key:
        name: login-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: login_html_repo_host
          ServiceInstanceName: login-html5-service
          ServiceKeyName: login-repo-host-key
          sap.cloud.service: login
        - Authentication: OAuth2UserTokenExchange
          Name: login_uaa
          ServiceInstanceName: login-xsuaa-service
          ServiceKeyName: login-uaa-key
          sap.cloud.service: login
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: login-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: login-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - login.zip
      name: login
      target-path: resources/
- name: login
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: login-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: login-destination-service
    service-plan: lite
- name: login-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: login-xsuaa-service
    service-plan: application
- name: login-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: login-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
